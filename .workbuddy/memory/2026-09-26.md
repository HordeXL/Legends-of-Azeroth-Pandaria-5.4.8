
## 任务 29824「斥候报告：锦鱼得水」机制排查
- 接/交任务：Shokia 55647（map 870, 1447,-547,352）；四环斥候报告 29730/29731/29823/29824 均无 quest_template_addon 行（无前置）。
- 目标：quest_objective type=0 kill credit objectId=55667 ×1；触发 = 琪麟 55667 SAI（event 64 OOC_LOS → action 33 CALL_KILLEDMONSTER, target=invoker），玩家靠近/进视线即计数。
- 无真护送逻辑：55667 无 waypoint/movement/addon，静止于刷新点 1426,-484,355（官方乘筏演出被简化）。
- 收尾：完成时核心 Player.cpp:16860 对玩家施放 RewardSpell=106409「Summon Nazgrim」（召唤 90 级纳兹戈林 54885，该 entry 无常驻刷新点）；系列中仅 29824 带 RewardSpell，为收尾环。

## 29824 护送流程溯源（官方数据对比）
- 29824 英文名 Scouting Report: Like Jinyu in a Barrel，前置 29823，接/交=Shokia 55647。
- 对比官方参考 dump（E:/desktop/LegionCore-7.3.5 项目依赖版本\world_548_20240722.sql，含 quest_template/SAI/waypoint 全表）：
  55667 官方 SAI 与本库逐字段一致（唯一 OOC_LOS→CALL_KILLEDMONSTER 行）；
  无 waypoint_data、无 GUID 级 SAI、无 quest accept 触发脚本、无载具配件；quest_poi 仅指路（含模板残留 Jade Forest 坐标）。
- 结论：官方 5.4.8 数据中该"护送"本来就是简化实现（走近即计数），无演出可还原；走动演出需自制 SAI 增强。

## 29824 自研护送演出实现
- 发现原机制实际不可达：官方行 event 64 在本核心是 GOSSIP_HELLO，琪麟 npcflag=0 无法交互 → 任务无法完成。
- 新增 2026_09_26_05_world_escort_quest_29824_kiryn.sql（幂等，已应用）：
  修姬亚 55647 event 19 接任务 → SET_DATA 通知琪麟 → 琪麟 WP_START 路径 5566700（步行穿营地，6 点，Z 线性插值 355.87→352.82）；
  event 40 各点台词（creature_text 组1-5）；
  event 58 终点 → CALL_KILLEDMONSTER(55667) target 17 PLAYER_RANGE(0,50) 发计数 + 台词组6 + 欢呼 → WP_START 5566701 跑步返回哨位（组7）。
- 关键代码确认：WP_START 动作作用于 source 而非 target，故用 SET_DATA 跨 NPC 通知；SET_DATA→AI()->SetData 触发 DATA_SET 事件（SmartScript.cpp:1080）。
- 本核心 SAI 编号与 LegionCore dump 不同：OOC_LOS=10、WP_REACHED=40、WP_ENDED=58、GOSSIP_HELLO=64、PLAYER_RANGE=17、CREATURE_RANGE=9。
- 需重启或 .reload smart_scripts/waypoint_data/creature_text。

## 29824 护送不触发修复
- 实测接任务后琪麟不动。根因：修姬亚 55647 的 creature_template.AIName 为空，
  本核心无 LoadSmartAIFromCreatureTemplate 自动机制 → 其 SAI（event 19）不加载，
  整条链路从源头断裂（开场白也不会出现）。
- 修复：SQL 增加 UPDATE creature_template SET AIName='SmartAI' WHERE entry=55647，已应用。
- 验证过全链路代码：SmartAI::QuestAccept→event19（SmartAI.cpp:812）；SET_DATA(45)→
  target AI()->SetData（SmartScript.cpp:1080）→ SmartAI::SetData→DATA_SET（SmartAI.cpp:758）；
  参数语义全匹配：DATA_SET=Id,Value；WP_REACHED/ENDED=PointId,pathID；
  WP_START=run,pathID,canRepeat,quest,despawnTime,reactState；CREATURE_RANGE=entry,minDist,maxDist。
- 生效必须重启 worldserver：waypoint_data 无 reload 命令，且已 spawn 的修姬亚
  reload creature_template 不会重建 AI。

## 29824 护送第二次修复（路径写错表）
- 现象：重启后接任务开场白正常（event 19 链路已通），但琪麟不动。
- 根因：本核心 SmartAI 路径加载自 `waypoints` 表（WORLD_SEL_SMARTAI_WP:
  SELECT entry, pointid, position_x/y/z, orientation, delay FROM waypoints），
  而非 TC master 的 waypoint_data。GetPath(5566700) 为空 → StartPath 静默 return。
- 修复：路径迁入 waypoints 表（12 行），清理 waypoint_data 误写行；SQL 同步更新。
- 经验：本核心 SAI 路径用 waypoints 表，delay 单位 ms，列仅 entry/pointid/x/y/z/orientation/delay/point_comment。
- 需再次重启 worldserver（waypoints 无 reload）。

## 29824 护送终点崩溃修复
- 现象：琪麟走完、说完终点台词后 worldserver 崩溃（Crashes dump 0 字节无堆栈）。
- 根因（源码确认）：SmartAI::EndPath 第212行触发 WAYPOINT_ENDED 事件时，
  原脚本 id13 同步 WP_START(返回路径) → StartPath 检测到 escort 状态未清
  （EndPath 214行才清）→ 递归 StopPath→EndPath 再次触发事件，且 215-218 行
  会把新路径 mWayPoints/mLastWP 置空 → 悬垂指针/递归崩溃。
- 修复：id13 改 action 67 CREATE_TIMED_EVENT(id=2, 4s, repeat=0)，
  新增 id15 event 59 TIMED_EVENT_TRIGGERED(2) → WP_START(返回路径)。
  定时触发时 EndPath 已返回、状态机干净，安全。已应用并回读。
- 本核心可用延迟机制：action 67 创建 / event 59 触发；action 69 MOVE_TO_POS 存在，
  MOVEMENT_INFORM 事件不存在。

## 29824 护送路线剧情化 + 场景演出
- 新路线：哨位(1426,-485) → 东南河滩 8 点 → 终点(1487,-546.5,343)，
  可望见 Riverstride Jinyu 营地(1523,-547)，全程距敌怪 35+ 码不进仇恨；Z 靠 mmaps 寻路修正。
- 场景演出（SAI 全实现）：点3 台词+召唤 2 只 55828 Young Alliance Soldier
  幻影战俘（summonType=3, 20s）；点4 施放 88611 盗贼烟雾弹（castFlags=2 TRIGGERED 零消耗）；
  点5/6 EMOTE 27 指向锦鱼人/猢狲；北面 59296 Lazy Hozen ×19、河滩锦鱼人为真实背景。
- 台词点号随 8 点路径调整：WP_REACHED 点2-6 对应 g1-g5。
- 风险备注：88611 若因职业限制施放失败则无烟幕（静默），可换其他视觉法术。
- 需重启 worldserver 生效。

## 29932「青龙寺」对话完成 → 场景 + 传送实现
- 需求：与青龙寺入口大贤者易冈（Elder Sage Wind-Yi, 57242）对话完成任务目标时，播放场景动画并传送到 (870, 918.029785, -2596.960938, 181.272446, O=-1.177)。
- DB 现状：quest_objective type=0 objectId=57290（Kill Credit: Breadcrumb to Jade Temple）；57242 SAI event 64(GOSSIP_HELLO) → action 33 CALL_KILLEDMONSTER(57290) target 17；交任务 NPC=Rain-Zhu 56782。原先无任何演出/传送。
- 实现方式：zone_the_jade_forest.cpp 新增 QuestScript `quest_temple_of_the_jade_serpent`，钩 OnQuestStatusChange（ScriptMgr.h:924，属 QuestScript），quest==29932 且 newStatus==COMPLETE 时 PlayScene(244) + TeleportTo。
- 调用链验证：KilledMonsterCredit(Player.cpp:17892) → CompleteQuest(16512) → SetQuestStatus(17411) → sScriptMgr->OnQuestStatusChange(17419)。
- 场景常量 SCENE_TELEPORT_TO_JADE_TEMPLE=244（scene_template 表中 package 524 scene_why_do_we_fight）；可用 .scene play <id> 测试后改常量。
- 已注册 AddSC_jade_forest()；需重编 worldserver 生效。本机 MySQL root/root，world 库（后续查库直接用 pymysql，凭据 root/root）。

## Release 编译 29932 脚本
- 首次编译报 C3668：本核心 OnQuestStatusChange 挂在 QuestScript（ScriptMgr.h:924，ScriptObject 派生）而非 PlayerScript，基类改 QuestScript 后通过。
- 环境坑：MSBuild 报 MSB6001 "已添加项 HTTP_PROXY/http_proxy"——shell 同时有大写和小写代理变量，需 `env -u http_proxy -u https_proxy` 再调 cmake。
- CMake 路径：VS2022 自带 "C:/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe"。
- Release 编译成功，产出 Build/bin/Release/worldserver.exe（scripts.lib 重新链接）。

## 29932 改用纯 SAI 方案（撤销 C++）
- 用户选择 SAI 方案：C++ QuestScript 钩子已从 zone_the_jade_forest.cpp 撤销并重编 Release（避免双触发）。
- 新增 sql/updates/world/2026_09_26_06_world_sai_quest_29932_jade_temple_teleport.sql（幂等，已应用）：
  - id0 (event 64) action 33 credit 57290，link=1；
  - id1 event_type=61(LINK) action 201 PLAY_SCENE_ID(244, apply=1)，target 7；
  - id2 event_type=61(LINK) action 62 TELEPORT(map 870)，target 7，坐标取行内 target_x/y/z/o=918.029785/-2596.960938/181.272446/-1.177。
- 关键机制确认：
  - link 链：事件行动作执行后 SmartScript.cpp:2542 找 id==link 且 event_type==SMART_EVENT_LINK(61) 的行顺序执行；
  - 本核心 SMART_TARGET_ACTION_INVOKER=7（GOSSIP_HELLO 时 invoker=对话玩家，SmartAI.cpp:794）；
  - target 17 是 PLAYER_RANGE（影响范围内所有玩家），勿用于单人传送；
  - action 201 需 param2=1(apply)；action 62 对玩家用 e.target.x/y/z/o + action_param1=mapID；
  - SAI 加载时 target 坐标列无条件填入 e.target（SmartScriptMgr.cpp:283），与 target_type 无关。
- 行为差异：SAI 挂 gossip hello，任何与易冈对话的玩家都会播场景+传送（更贴官方"传送进寺"选项）；可用 conditions 表 (IsObjectMeetingSmartEventConditions, SmartScript.cpp:2554) 挂任务状态条件门控。
- 生效：.reload smart_scripts 即可，无需重启。

## 最终版：场景 66 + 双分支（SAI 条件 + SceneScript 结束传送）
- 用户确认正确场景 ID=66（package 200）。需求改为双分支：有任务 29932 对话→credit→场景 66→场景结束传送；无任务→gossip 选项"送我到青龙寺庭院。"直接传送。
- 关键机制（本核心源码核实）：
  - SAI LINK 链立即执行无延迟、无场景结束事件、SetScript9 只挂 Creature/GO → "场景结束后传送"只能用 C++ SceneScript OnSceneComplete（SceneMgr.cpp:165 回调）。
  - SMART_EVENT 条件查找键 SourceEntry = 事件id+1（ConditionMgr.cpp:1116），SourceGroup=entryorguid，SourceId=source_type。
  - GOSSIP_SELECT(62) 事件 param1=MenuID、param2=OptionID（gossipListId 即 OptionID，GossipDef.cpp AddMenuItem 以 OptionID 为键）。
  - CONDITION_QUESTSTATE(47) state 掩码：1未接/2完成/8进行中/32失败/64已奖励；CONDITION_QUESTTAKEN(9) 仅 INCOMPLETE。
- DB 最终状态（sql/updates/world/2026_09_26_06_world_sai_quest_29932_jade_temple_teleport.sql，已应用）：
  - smart_scripts 57242: id0(64→33 credit 57290, target 7, link=1)+条件(22/57242/1/0, QUESTSTATE 29932 mask 10)；id1(61→201 PLAY_SCENE 66, target 7)；id3(62 param 13324/0→62 TELEPORT 870 918,-2597,181,o-1.177, target 7)。
  - conditions: gossip 选项 15/13324/0/0 负条件 QUESTSTATE 29932 mask 10。
  - gossip_menu_option 13324/0 "送我到青龙寺庭院。" OptionType=1(GOSSIP_OPTION_GOSSIP)。
  - scene_template SceneId=66 ScriptName='scene_jade_temple_teleport'。
- C++: zone_the_jade_forest.cpp 新增 scene_jade_temple_teleport : SceneScript，OnSceneComplete+OnSceneCancel 都 TeleportTo(870,918,-2597,181,-1.177)，已注册 AddSC。
- 坑：pymysql 简易 SQL 解析器遇 "语句; -- 行内注释" 会拼坏后续语句；gossip_menu_option INSERT 需含 VerifiedBuild 共 14 列。
- Release 编译成功（zone_the_jade_forest.cpp 重编 + worldserver.exe 重链）。生效：重启或 .reload smart_scripts + .reload conditions。

## NPC 56836 大贤者雷霆 gossip 传送
- 56836 (Elder Sage Thunder-Lei) 原为空白模板：npcflag=0、gossip_menu_id=0、无 AI；刷新点 guid 4000121，(953.638, -2486.94, 180.443, o 4.39669)，map 870，phaseMask 0x4000001。
- 新增 sql/updates/world/2026_09_26_07_world_npc_56836_gossip_teleport.sql（已应用）：模板开 npcflag=1 + AIName='SmartAI' + 菜单 13325（TextID=1 通用问候）；选项 OptionID 0 "我想返回青龙寺庭院。"（OptionType=1）；SAI GOSSIP_SELECT(62, param 13325/0) → TELEPORT 870 954,-2487,181,o 4.39669，target 7。
- 纯 DB 无需重编译；AIName/npcflag 变更需重启或 .reload creature_template + 重 spawn NPC。

## 修复：场景跳过黑屏 + 崩溃（延迟传送）
- 崩溃日志 F:/LOA-Pandaria-5.4.8-Release/Crashes/：22:06 C0000005（旧 SAI 立即传送版）、23:21 C0000420=WPAssert（场景回调立即传送版，Errors.cpp:40 RaiseException）。F 盘 exe 与 E 盘 Build/bin/Release md5 一致（7ce80d385d8902643093c7a4f898ac20），但 Release 无 PDB（TC CMake 仅 RelWithDebInfo 生成），无法符号化；Server.log 无断言文本（Assert 只打控制台）。
- 根因：OnSceneComplete/OnSceneCancel 在场景包处理上下文（WorldSession::Update）里同步调用 TeleportTo —— 客户端未退出场景/电影状态 → 黑屏；服务端在包处理中途传送 → 断言/访问冲突。
- 修复：zone_the_jade_forest.cpp 新增 JadeTempleDelayedTeleportEvent(BasicEvent)，持有 ObjectGuid，Execute 时经 ObjectAccessor::FindConnectedPlayer 解析（空安全）+ IsInWorld/!PlayerLogout 守卫后 TeleportTo(870,918,-2597,181,-1.177)；scene_jade_temple_teleport 的 Complete/Cancel 改为 player->m_Events.AddEventAtOffset(event, 2000) 延迟 2 秒（Player::Update 上下文执行）。新增 include ObjectAccessor.h/EventProcessor.h。
- 注意：本核心 Object.h 无 IsDuringRemoveFromWorld()；Player 无 m_Events（在 Unit.h:2096）。Release PDB 缺失如需符号化可改用 RelWithDebInfo 跑服复现。
- Release 重编成功。测试路径：对话→场景 66→播完或提前跳过 → 2 秒后传送到庭院。

## 坐标调整
- 56836 对话「我想返回青龙寺庭院。」传送坐标 954,-2487,181 → 992,-2449,167（O 仍 4.39669），SQL 文件 2026_09_26_07 已同步改，已应用 world 库并回读校验。

## 提交
- 提交 b0381c4e「feat(quest 29932): 青龙寺任务链场景动画与传送功能」：zone_the_jade_forest.cpp + SQL 06/07，3 文件 +149 行。未提交项：sql/updates/world/2026_09_26_05_world_escort_quest_29824_kiryn.sql（非本次会话产物，未纳入）、.workbuddy/ 目录。
